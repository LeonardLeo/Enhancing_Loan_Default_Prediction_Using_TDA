# Default_Parameters

Tabular machine-learning baseline with **library default hyperparameters**.

- `1_ML_Default_Parameters` — old Experiment 1, library-default classifiers on the processed table.
- `2_ML_Tuned_Parameters` — old Experiment 2, GridSearchCV on the same tabular features.
- Does not generate landmarks or barcodes.
