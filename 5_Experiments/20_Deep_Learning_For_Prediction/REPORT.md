# Experiment 20 — Folder name vs what the code actually does

## In one sentence

The folder is called “Deep Learning”. **There is no TensorFlow/PyTorch model.**

## Who this is for

The Statlog script is persistent homology with `generate_landmark_sets_v2` (imbalanced file counts), same family as Experiment 14. Treat results as another PH variant, not as a neural-net baseline.

A real deep-learning experiment would live here later. Until then, do not quote this folder as “we tried a neural net”.

## Datasets

All six have a script in this tree; they follow the PH variant, not a network.

## Results

`6_Results/20_Deep_Learning_For_Prediction/{Folder}/`
